--  1 Create a new database called salesDB
CREATE DATABASE IF NOT EXISTS salesDB;
-- Creates the database 'salesDB' only if it doesn't already exist, maybe because i create 2 times, i have to make sure that doesnt exist.

-- 2 Drop (delete) the database called demo
DROP DATABASE IF EXISTS demo;
-- Drops or delete the database 'demo' only if it exists.


